// we have to define function by this method
function sum(a, b) {
    return a + b;
}
console.log(sum(20, 90));
// we define type by using:
function mul(a, b) {
    return a * b;
}
console.log(mul(20, 90));
